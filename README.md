# dsh-nitrogen · 氮元素

## 工具

- `element_info`：返回氮元素（Nitrogen）完整资料：名称、元素符号、原子序数、相对原子质量、类别、族、周期、常温状态、电子构型。
- `element_field`：按字段名查询氮元素（Nitrogen）的单项资料（可用于流程取数）。

纯 Node 实现，无网络依赖。

```bash
dsh plugin add github:uckkk/dsh-nitrogen
```
